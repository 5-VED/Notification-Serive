syntax = "proto3";

package services.notification;

import "events/user.proto";   // relative path inside Protos

service NotificationService {
  rpc SendLoginEmail (events.user.UserLoggedIn) returns (SendEmailResponse);
}

message SendEmailResponse {
  bool success = 1;
  string message_id = 2;
}
